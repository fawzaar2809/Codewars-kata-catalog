function getMean(arr, x, y) {
  /*   const first = arr.reduce((a, b) => a + b, 0) / arr.length;

  const second = (x + y) / 2;

  const res = (first + second) / 2;

  return x > 1 && y > 1 && x <= arr.length && y <= arr.length ? res : -1; */
}
